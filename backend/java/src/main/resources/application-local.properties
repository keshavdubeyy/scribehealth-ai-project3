# ============================================================
# LOCAL OVERRIDES — DO NOT COMMIT (gitignored)
# ============================================================
# Paste your Supabase direct connection string here.
# Find it in: Supabase Dashboard → Project Settings → Database → Connection string → JDBC
# Format: jdbc:postgresql://db.<project-ref>.supabase.co:5432/postgres
spring.datasource.url=jdbc:postgresql://db.<your-project-ref>.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=<your-db-password>
